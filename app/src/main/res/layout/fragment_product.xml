<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>
        <import type="android.view.View"/>
        <variable
            name="buttonEnabled"
            type="Boolean" />
        <variable
            name="product"
            type="com.ft.ftchinese.ui.paywall.UIProduct" />
        <variable
            name="yearPrice"
            type="com.ft.ftchinese.ui.paywall.Price" />
        <variable
            name="monthPrice"
            type="com.ft.ftchinese.ui.paywall.Price" />
    </data>

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <TextView
                android:id="@+id/product_heading_tv"
                style="@style/OrigamiText.ProductHeading1.Center"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="16dp"
                android:layout_marginEnd="16dp"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="16dp"
                android:text="@{product.heading}"
                android:background="@drawable/bottom_border_teal"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toTopOf="@id/pre_discount_price"/>

            <TextView
                android:id="@+id/pre_discount_price"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@{yearPrice.originalPrice}"
                android:textAlignment="center"
                android:visibility="@{yearPrice.originalPrice == null ? View.GONE : View.VISIBLE}"
                app:layout_constraintEnd_toEndOf="@+id/product_heading_tv"
                app:layout_constraintStart_toStartOf="@id/product_heading_tv"
                app:layout_constraintTop_toBottomOf="@id/product_heading_tv"
                app:layout_constraintBottom_toTopOf="@id/yearly_price_btn"/>

            <Button
                android:id="@+id/yearly_price_btn"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:text="@{yearPrice.amount}"
                android:enabled="@{buttonEnabled}"
                app:layout_constraintTop_toBottomOf="@id/pre_discount_price"
                app:layout_constraintStart_toStartOf="@id/product_heading_tv"
                app:layout_constraintEnd_toEndOf="@id/product_heading_tv"
                app:layout_constraintBottom_toTopOf="@id/monthly_price_btn"/>


            <Button
                android:id="@+id/monthly_price_btn"
                style="@style/AppButton.OutlinedButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:text="@{monthPrice.amount}"
                android:enabled="@{buttonEnabled}"
                android:visibility="@{monthPrice == null ? View.GONE : View.VISIBLE}"
                app:layout_constraintTop_toBottomOf="@+id/yearly_price_btn"
                app:layout_constraintStart_toStartOf="@id/product_heading_tv"
                app:layout_constraintEnd_toEndOf="@id/product_heading_tv"
                app:layout_constraintBottom_toTopOf="@id/product_benefits_tv"/>

            <TextView
                style="@style/OrigamiText.Body1"
                android:id="@+id/product_benefits_tv"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:layout_marginBottom="16dp"
                android:lineSpacingExtra="8sp"
                android:text="@{product.description}"
                app:layout_constraintStart_toStartOf="@id/product_heading_tv"
                app:layout_constraintEnd_toEndOf="@id/product_heading_tv"
                app:layout_constraintTop_toBottomOf="@id/monthly_price_btn"
                app:layout_constraintBottom_toTopOf="@id/product_small_print"/>

            <TextView
                android:id="@+id/product_small_print"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginBottom="16dp"
                android:text="@{product.smallPrint}"
                android:visibility="@{product.smallPrint == null ? View.GONE : View.VISIBLE}"
                app:layout_constraintStart_toStartOf="@id/product_heading_tv"
                app:layout_constraintEnd_toEndOf="@id/product_heading_tv"
                app:layout_constraintTop_toBottomOf="@id/product_benefits_tv"
                app:layout_constraintBottom_toBottomOf="parent"/>



        </androidx.constraintlayout.widget.ConstraintLayout>
    </androidx.cardview.widget.CardView>

</layout>

